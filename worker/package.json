{
  "name": "notebook-api",
  "version": "1.0.0",
  "description": "Cloudflare Workers API for web-notepad",
  "main": "src/index.js",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
