{
  "name": "notebook-api",
  "version": "2.0.0",
  "description": "Cloudflare Workers backend + static assets for web-notepad",
  "main": "src/index.js",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
